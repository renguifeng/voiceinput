# Architecture / 开发笔记

## 设计思路

### 为什么用 FunASR 2pass 模式

FunASR 的 2pass 模式同时返回：
- `online` 结果：低延迟、粗略识别（实时显示）
- `offline` 结果：高精度、最终识别（替换为准确文字）

用户说话时看到实时字幕，说完后自动替换为准确结果，体验接近商业输入法。

### 为什么用 Clipboard + Ctrl+V 而不是直接键盘输入

中文输入如果逐字 `SendInput`，会被输入法拦截（IME 会吞字符）。剪贴板粘贴绕过了 IME，对任意输入框都稳定。

代价是会覆盖用户剪贴板，所以录音开始前保存、结束后恢复。

### 为什么中间结果用字幕条而不是直接粘贴

中间结果频繁变化，直接粘贴会造成大量撤回操作。用半透明字幕条悬浮显示，最终结果再一次性粘贴。

## 已知限制

- Windows 专属（Win32 API: clipboard, SendInput）
- 依赖本地 FunASR 服务，部署有门槛
- 剪贴板方案会在录音期间短暂占用剪贴板
- 没有标点符号自动补全（依赖 FunASR 模型能力）

## 未来方向

- [ ] 一键安装包（内置 FunASR Docker 拉起）
- [ ] 云端 API 模式（免部署）
- [ ] 标点符号自动补全
- [ ] macOS / Linux 支持
- [ ] 声音活动检测（VAD），静音自动停止
